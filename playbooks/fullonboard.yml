---
# playbooks/full_onboarding.yml
- name: Full Onboarding for Web Hosting Client
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - "{{ client_file }}"

  roles:
    - role: directadmin
    - role: cloudflare
  # - role: monitoring

